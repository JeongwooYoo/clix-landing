/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // optimizeCss disabled (was using experimental.optimizeCss which required 'critters')
};

export default nextConfig;
